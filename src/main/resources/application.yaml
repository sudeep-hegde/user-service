spring:
  application:
    name: User-Service
  datasource:
    url: jdbc:postgresql://localhost:5432/shopperzaar
    username: quizdb
    password: quizdb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  private.key: classpath:app.key
  public.key: classpath:app.pub